# TC-16 — Testing Performance And Edge Cases

```text
Execute TC-16. Add/complete tests and harden edge cases for Phase 1.

Cover at minimum:
- ruleset adapter calculations/contracts;
- Character Builder critical path;
- privacy visibility logic;
- Encounter template vs Combat Instance separation;
- initiative/turn advancement;
- HP damage/heal/undo state;
- public vs secret roll visibility;
- connection/reconnect UI state;
- key responsive smoke coverage where the test stack supports it.

Also inspect bundle/runtime warnings, unnecessary rerenders in combat-heavy surfaces, list keys, hydration issues, and obvious performance bottlenecks.

Acceptance: relevant automated checks pass and no known critical Phase 1 interaction is untested or manually undocumented.
```
