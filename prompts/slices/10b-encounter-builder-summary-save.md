# TC-10b — Encounter Builder summary, validation, and save

Execute only this slice after TC-10a. Add encounter summary, ruleset-provided difficulty metrics when available, validation/warnings, autosave feedback, save/edit/duplicate behavior, and large-encounter states.

Keep EncounterTemplate immutable from future CombatInstance runtime changes.

Acceptance: empty, populated, and large encounter states are handled; save feedback is subtle and reliable.
