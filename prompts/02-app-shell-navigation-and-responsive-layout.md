# TC-02 — App Shell Navigation And Responsive Layout

```text
Execute TC-02. Implement the global application shell and responsive navigation from the approved design.

Scope:
- DM desktop/tablet shell;
- Player mobile shell;
- global/sidebar navigation;
- campaign sub-navigation pattern;
- top bars/toolbars;
- contextual right-side panel/drawer architecture;
- mobile bottom navigation where shown/appropriate;
- route skeletons for Phase 1 screens;
- loading/empty routing states.

Use the approved Claude Design as source of truth. Keep future Phase 2/3 extensibility without surfacing fake disabled features.

Acceptance: shell works at desktop/tablet/mobile breakpoints, context panel pattern is reusable, keyboard focus order is sane, and existing design system primitives are reused.
```
